<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".CocktailDBApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/cocktail_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/cocktail_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.CocktailDB"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">


        <activity
            android:name=".screen.splash.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".screen.home.HomeScreenActivity"
            android:exported="false" />

        <activity
            android:name=".screen.categorydetail.CategoryDetailActivity"
            android:exported="false" />

        <activity
            android:name=".screen.authenticate.login.LoginActivity"
            android:exported="false" />

        <activity
            android:name=".screen.authenticate.register.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".screen.cocktaildetail.CocktailActivity"
            android:exported="false" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>
